extends ../layout/layout.pug

block content 
    .row 
        .col-md-3 
                img(src=`/img/${product.image}`).img-fluid
        .col-md-9 
            p.mb-3 Name: #{product.name}
            hr
            a(href="").btn.btn-link.p-0.mb-3 Fiyat
            .mb-3 
                h4.text-primary.mb-3 #{product.price} TL
                button(type="submit").btn.btn-primary.btn-lg Add to Cart

    .row 
        .col-md-12  
        hr
        h2.p-3 #{product.brand} 
