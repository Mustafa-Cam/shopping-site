extends ../layout/layout.pug

block styles 
    link(rel="stylesheet" href="/css/forms.css")
block content
    form(action="/edit-category" method="POST") 
            input(type="hidden",value=`${csrfToken}` name="_csrf")
            input(type="hidden" name="id" value=`${category._id}`) 
            .form-group.row
                label(for="name").col-sm-2.col-form-label Category name 
                .col-sm-10
                    input(type="text" name="name" value=`${category.name}`).form-control#name
            .form-group.row
                label(for="description").col-sm-2.col-form-label description
                .col-sm-10
                    input(type="text" name="description" value=`${category.description}`).form-control
            
            //- .form-group.row
            //-     label.col-sm-2.col-form-label category
            //-     .col-sm-10
            //-         select(type="text" name="categoryid").form-control#categoryid        
            //-             option(value="-1")
            //-             each category in categories
            //-                 option(value=`${category.id}` selected=category.id==product.categoryId) #{category.name}  
             
            .form-group.row
                .col-sm-10.offset-sm-2                       
                    input.btn.btn-primary(type="submit" value="save product")
